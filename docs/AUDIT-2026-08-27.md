# Audit — 27 August 2026

A full pass over Supabase, Netlify, GitHub and the live site, written down
because this has been rebuilt more than once and the findings kept getting lost.

Read `## The pattern` first. It explains most of what follows.

---

## The pattern

**Successive rebuilds registered buttons on the delegated click listener and
never wrote the handler branch behind them.** `app.js` has one listener at the
bottom whose `closest()` selector lists every clickable `data-*` attribute. Being
in that list makes a click *reach* the listener. It does not make anything
happen. A rebuild that adds a button to a panel and adds it to the selector, but
stops before the `if (btn.dataset.x)` branch, produces a button that looks
finished, is styled, has a cursor — and silently does nothing.

Nine were found this way. The whole Letters panel was one of them.

**How to check, in one command.** Run this before believing any panel works:

```
node -e '
const s=require("fs").readFileSync("public/devdash/app.js","utf8");
const sel=new Set(); for(const m of s.match(/e\.target\.closest\("([^"]+)"\)/)[1]
  .matchAll(/\[data-([a-z0-9-]+)\]/g)) sel.add(m[1]);
const h=new Set(); for(const m of s.matchAll(/btn\.dataset\.([A-Za-z0-9]+)/g)) h.add(m[1]);
const c=x=>x.replace(/-([a-z0-9])/g,(_,l)=>l.toUpperCase());
const dead=[...sel].filter(x=>!h.has(c(x)));
console.log(dead.length ? "DEAD: "+dead.join(", ") : "no dead buttons");'
```

---

## 1. Dead buttons found and fixed

| Panel | Button | Backend action it should call | Was |
| --- | --- | --- | --- |
| Letters | Write the letter | `letters.generate` | dead |
| Letters | Mark sent to Debi | `letters.mark_sent` | dead |
| Letters | Try again | `letters.list` | dead |
| Asks | **Approve** | `asks.approve` | dead |
| Asks | Unstage | `asks.unstage` | dead |
| Asks | Dismiss | `asks.dismiss` | dead |
| Asks | Put it back | `asks.restore_task` | dead |
| Follow ups | Dismiss | `asks.followup_dismiss` | dead |
| Follow ups | Mark answered | `asks.followup_answered` | dead |
| Gala 2026 | the four sub-tabs | client-side | dead |
| Gala 2026 | Download *list* | client-side `saveCSV` | dead |

Two consequences worth naming:

- **Letters**: no letter had ever been generated from the dashboard. One row in
  `letter_drafts` (created out-of-band), and `letter_id` null on all 44
  unthanked gifts back to March. "Letters missing from August" was this.
- **Asks**: `approve` is what moves an answer to `staged`. Nothing could ever
  reach `staged`, so "Create Gmail draft" — which assembles the staged answers
  into one mail in Debi's numbering — had nothing to assemble. The Asks
  workflow stopped dead at the draft.

`saveCSV` and `letterFor` showed as unused variables in ESLint for exactly this
reason. **An unused helper in this file is a symptom, not lint noise.**

---

## 2. Nothing had reached Bloomerang, and could not have

`crm_inbox`: 5 `needs_review`, 2 `rejected`, **0 `pushed`** — ever.

Three writes exist in `crm_write_ledger`, all development, all 15–16 Aug:

| Natural key | Type | Result |
| --- | --- | --- |
| `constituent:14f9…` | constituent | Bloomerang 18803713 → account 4113 |
| `note:new:18803713` | note | Bloomerang 18804737 |
| `attachment:d730…` | attachment | Bloomerang 49, account 165 |

**Root cause.** `gift-scan` stages every donation, sponsorship and grant into
`crm_inbox` and **never sets `match_constituent_id` or `match_method`.** There is
no matching step in it at all — unlike `asks.to_bloomerang`, which calls
`matchConstituent()` and records the result.

So every money row arrived marked "No constituent match", and
`bloomerang.approve` refuses before it does anything:

```
if (payload._accountNumber == null)
  return json({ error: "No constituent matched. Set _accountNumber or create one." }, 400);
```

Verified live on the Micron row — returns exactly that 400 and mutates nothing.

**And the matches exist.** Every queued donor resolves through the canonical
matcher:

| Donor | Card said | `bloomerang.match` returns |
| --- | --- | --- |
| Micron Technology | no match | **3423** (`account_map_name`, 0.65) |
| Jack R. Anderson Foundation | no match | **210** (`account_map_name`, 0.65) |
| Celine Healey | no match | **210** (`sender_map:snapshot`, **1.0**) |
| Victor Hoskins | no match | **2968** (`account_map_name`, 0.65) |

Creating a constituent from that screen would have made a **duplicate of a donor
already on file** — which is how this database came to hold two to four records
for most donors (see §4).

**Fixed in the dashboard**: an unmatched card now looks the match up and shows it.
A confident hit (confirmed sender mapping, or a unique email — score ≥ 0.9) fills
the account number in. A **name-only** hit is offered as "Likely #N · use it" and
is deliberately *not* filled in, because a wrong account number writes a gift
onto the wrong donor's record.

**Fixed in the backend, 28 Aug — `gift-scan` v3.** The scanner now resolves the
donor before it stages the gift, so the match is right in the data and not only
on screen. Two earlier deploys did not land: the function is ~30KB and the upload
came back malformed, so it was **rejected by input validation and nothing was
deployed** — the live scanner was never touched, and the retry was safe. (The
failure mode worth guarding against is the other one: a paste that truncates
*silently* and deploys half a function, which the post-deploy check below
catches. It was run against v3 and the new counters came back live.)

Two details mattered, and both are in the shipped version:

- Mirror the *cheap half* of `bloomerang.matchConstituent` (local tables only —
  `crm_sender_map`, `constituent_clusters`, `crm_account_map`). `bloomerang`'s
  own `requireUser` does **not** accept the service role key, so cron cannot
  call it over HTTP.
- Match on `extraction.donor_email`, **never on the message sender.** The
  Micron grant arrived from `debi@sparcsolutions.org`; matching on the sender
  would file the gift against Debi's own record.

### The patch, as shipped

The full behaviour is written up in `docs/BACKEND.md` under "It resolves the
donor before staging". What follows is the matcher itself, added above
`// ------ the model`:

```ts
const normName = (s: string) => s.toLowerCase().replace(/[^a-z ]/g, "").trim();

async function matchConstituent(email?: string | null, name?: string | null) {
  if (email) {
    const { data } = await db.from("crm_sender_map").select("constituent_id")
      .ilike("email", email).maybeSingle();
    if (data) return { account_number: data.constituent_id, method: "sender_map", score: 1 };
    const { data: m } = await db.from("crm_account_map").select("account_number")
      .ilike("primary_email", email).limit(2);
    if (m?.length === 1) return { account_number: m[0].account_number, method: "account_map_email", score: .95 };
  }
  if (name) {
    const { data: cl } = await db.from("constituent_clusters")
      .select("survivor_account, record_count").eq("norm_name", normName(name)).maybeSingle();
    if (cl) return { account_number: cl.survivor_account,
      method: "cluster_survivor(" + cl.record_count + " records)", score: .7 };
    const { data: n } = await db.from("crm_account_map").select("account_number")
      .ilike("full_name", name).limit(3);
    if (n?.length === 1) return { account_number: n[0].account_number, method: "account_map_name", score: .65 };
    if (n?.length) return { account_number: null, method: "ambiguous_name", score: 0 };
  }
  return { account_number: null, method: "no_match", score: 0 };
}
```

Then in `scan()`, immediately before the `crm_inbox` insert:

```ts
// The donor's email, NOT `from`. See the warning above.
const match = await matchConstituent(ex.donor_email ?? null,
  ex.donor_organization ?? ex.donor_name ?? null);
```

and two fields inside that insert:

```ts
match_constituent_id: match.account_number,
match_method: match.method + (match.score ? " @" + match.score : ""),
```

**Verify after deploying**: fetch the function back and confirm the length is
~30KB and that `ANSWER`-side markers survived — the `SYSTEM` prompt, `classify`,
`enrich`, and the one-gift-per-thread `sibling` lookup. A 30KB upload can
truncate silently; a malformed one is rejected outright and deploys nothing.

**A score of 0.65 is a suggestion, not a match.** If the column is populated
from a name-only hit, the dashboard renders a confident green "Matched" and the
"use it" confirmation is skipped. Either keep name-only hits out of the column,
or teach the card to read `match_method` and treat a `account_map_name` /
`cluster_survivor` match as needing confirmation.

**Backfilled by hand, 27 Aug**: only the one row that resolves at high
confidence — the Jack R. Anderson Foundation grant → account 210, via a
confirmed sender mapping *and* a unique primary email, both agreeing. Account
210's `full_name` is "Jack R. Anderson Foundation" with Celine Healey as its
contact, so the target is right.

The other three were left unmatched **on purpose.** Micron (3423) and Victor
Hoskins (2968) resolve on name alone; writing those into the column would make
the card read a confident green "Matched" and bypass the "use it" confirmation
that exists precisely so a name-only guess cannot file a gift against the wrong
donor.

**One booby-trap worth knowing.** Micron's extracted `donor_email` is
`noreply.grants@benevity.com` — Benevity's no-reply relay, not Micron. It
matches nothing today. If that address ever lands in `crm_sender_map` it will
map every Benevity-routed corporate grant to whichever donor it was first
confirmed against. Do not confirm a sender mapping for a no-reply relay.

---

## 3. Features deployed but reachable from no button

Found by diffing the actions each function accepts against the actions `app.js`
calls.

| Action | State |
| --- | --- |
| `bloomerang.upsert_constituent` | existed since 16 Aug; only offered on asks-routed rows. **Now offered on any unmatched row.** |
| `docs.revise` | applies Debi's tracked changes and comments and reports what it could not do. Reachable from nothing. **Now wired to "Make her edits".** |
| `asks.dismiss_task` | no caller |
| `bloomerang.match` | no caller — **now used for the match lookup above** |
| `donor-ops.update` / `dismiss` / `restore_dismissed` / `note` / `note_delete` | **no caller.** These are the flag-resolution actions. CLAUDE.md describes the product as flagging incomplete records and letting staff resolve them; that path is not wired to anything. Not addressed in this pass — it needs its own decision about where it belongs now that the nine tabs became seven. |
| `tasks.check` / `add` / `update` | no caller — expected, the Today tab was removed |
| `bloomerang.approve_sponsorship` | no caller |

`app.js` also still carries a handler for `askBlm` with **no button rendering
it**, and one for `data-quote` likewise. Dead code in the other direction.

---

## 3b. The letters loop was open — two tables, never joined

`letters.generate` writes **`letter_drafts`**. `donation_ack_status.thanked`
reads **`thank_you_letters`.sent_to_debi_at**:

```sql
LEFT JOIN thank_you_letters l ON l.donation_id = d.id OR donation_fingerprint(...) = ...
... l.sent_to_debi_at IS NOT NULL OR COALESCE(b.acknowledged,false) AS thanked
```

Different tables. So a letter written from the dashboard could never take the
gift off "Owed a thank-you letter", and the same donor would be written to again
and again. `docs/BACKEND.md` recorded this gap on 26 Aug and it was never closed.

A second bug on the same seam: `owedRow` sent `donation_id: g.donation_id`, but
the view exposes the donation as **`id`** — there is no `donation_id` column. So
every letter went off unlinked even where the join would have worked.

**Both fixed.** `generate` and `draft_batch` now also record the letter in
`thank_you_letters`, and the draft to Debi stamps `sent_to_debi_at`.

Two CHECK constraints on that table nearly broke this, and are worth writing
down because neither takes the vocabulary the letters function works in:

| Column | Allowed | Not |
| --- | --- | --- |
| `category` | `General`, `Summit`, `Gala` | a `gift_type` like `gala_sponsorship` |
| `status` | `unsent`, `sent_to_debi`, `mailed` | `letter_drafts`' own `drafted_to_debi` |

Writing the raw values fails the insert outright, which inside `generate`'s
try/catch would have marked every letter `failed` with a 500. Caught by probing
the insert shape against the live table before deploying.

## 4. Data reality

| | |
| --- | --- |
| `crm_account_map` | 2,021 accounts. **No address columns** — account number, Bloomerang id, full name, primary email, synced_at. Nothing else. |
| `crm_sender_map` | 1,450 confirmed email → constituent mappings |
| `letter_drafts` | 1 |
| `thank_you_letters` | 9, all created 14 Aug; 6 say "No Gmail draft found" |
| `donation_ack_status` | 44 unthanked gifts, March–August. `letter_id` null on every single one. |
| `crm_inbox` | 7 |
| Open asks | several hundred (381 created by the 25 Aug backfill) |

**Duplicates.** Nearly every donor name matches 2–4 rows in
`crm_account_map`. A bare name match is therefore unsafe. `constituent_clusters`
exists for this and resolves ~74% of the donors owed a letter to a
`survivor_account`; it is what `bloomerang.matchConstituent` uses, and now what
the letter address lookup uses.

**Addresses are not in Supabase at all.** They are read live from Bloomerang
(`GET /constituent/{id}` → `PrimaryAddress`) at letter-generation time. Before
27 Aug every letter written from the dashboard went out with a name and no
address block.

---

## 5. Scheduled jobs — all running, most of them new

Eleven jobs, all `active`, last run all `succeeded`, zero failures recorded.
But see "Healthy is not the same as long-standing" below: most of them were only
created on 26–28 August, so a clean run history covers days, not weeks.

**`pg_cron` "succeeded" does not mean the HTTP call worked** — `pg_net` is
async, so a 401 still records success. This bit before: `cron_token` was expired
and three jobs 401'd silently. Two ways to check properly:

1. `net._http_response` — but it is pruned on a TTL, so only the last few hours
   survive. In the retained window: 4 calls, all 200.
2. **Whether the job had an effect**, which is the durable test.
   `task_scan_runs`: 3 runs and 22 tasks created today, 0 errors.

### Healthy is not the same as long-standing — 28 Aug

`cron.job_run_details` says when each job *first* ran, and the answer reframes
the whole "letters missing from August" question:

| Job | First run |
| --- | --- |
| `tasks-scan-0800` / `-1200` / `-1700` | 26 Aug |
| `sync-bloomerang-transactions` | 27 Aug |
| `gift-scan-0800` / `-1200` / `-1700` | **27 Aug** |
| `promote-donations` | 28 Aug |

**Nothing scheduled read Erica's mail for gifts before 27 August.** The job that
used to — an agent Routine called "Email Sweep", `0 8 * * *`, outside Supabase
entirely — last fired **15 August** and its `next_run_at` has been stuck at
16 August ever since. So 16–26 August had no gift scan of any kind.

That window is now covered. A `days:14` backfill run on 28 Aug examined 48
messages back to 14 Aug: 43 already decided by the 27 Aug runs, 5 newly judged,
**0 gifts found**. So the hole in `crm_inbox` for those days was never missed
gifts from email — it was a hole in the *decisions*, and it is closed.

The August gifts that really were missing came from the other side entirely:
Bloomerang, not email, and §9 is where that is worked out. The stalled "Email
Sweep" Routine still wants either restarting or retiring — the Supabase crons
have taken over its money path, but not its letter-status check or its RSVP and
tuition queues.

### First scheduled run of the new pipeline — 28 Aug, 07:45 UTC

`promote-donations` fired on its own for the first time: HTTP 200,
**0 promoted, 56 skipped, 0 unnamed**. Nothing new to promote is the right
answer — yesterday's six are staged and the two-sided dedupe held on the
re-run. The job is real, not just scheduled.

One thing the same morning did surface. `bloomerang-snapshot` reported
`rows_in_snapshot: 1676` against `total: 1677`, so the mirror was one
transaction short. Its `complete: false` is **not** the tell — the nightly run
reads the newest 400 by design and always says that. The tell is the two counts
disagreeing. A manual walk from `skip: 400` closed it (1677/1677,
`unmatched_accounts: 0`), and `donation-sync` over the repaired mirror promoted
nothing, so the missing row was not an unthanked gift.

Checking `bloomerang-ack` for the same thing found it: `bloomerang_acknowledgments`
also held **1676 of 1677**, and the same walk closed it. Both loaders page the
same endpoint the same way, so this belongs to the pattern rather than to one
function — whenever a Bloomerang mirror's row count and `total` disagree, walk it
from `skip: 400`. The acknowledgements table is the one that matters: `acknowledged`
is what says whether a donor has already been thanked, so a row missing from it is
a gift that cannot be *seen* as unthanked. Both comparisons are written up in
`docs/BACKEND.md`.

### The scanners are live and unattended — 28 Aug, 12:05 UTC

`gift-scan` v3's first scheduled run: HTTP 200, **0 errors**, and the three new
counters (`matched`, `needs_confirming`, `no_constituent`) present in the
response — so the deployed code is what cron is running, and nothing truncated.
17 messages examined, all already decided, nothing staged. The matcher has not
yet had a new gift to resolve; that is the next thing to watch for.

The task scan at 12:00 created two tasks from an 11:49 email, so that half of the
pipeline is finding real work the same morning it arrives.

By 16:05 that had become **13 new tasks for the day** (417–429), and the 16:05
gift scan came back 200 with 0 errors, 22 examined, 5 newly judged not a gift,
nothing staged. So the matcher still has not met a new gift; that remains the one
thing about v3 not yet proven in the wild.

Two of the day's tasks are letter blockers rather than outreach: **421** "Send
corrected version to Debi today" and **420** "Provide background context for the
$100 before Debi can sign letters".

### Answers are pre-drafted now — 28 Aug, 16:30 UTC

`asks-autodraft` is deployed and scheduled. Its first live run drafted 3 of 3,
0 refused, 0 failed, 20s. Task 420's draft is worth quoting, because it is the
behaviour to keep:

> I do not have the background on the $100 in this thread. I will pull together
> [description of what the $100 represents — e.g., minimum gift amount,
> processing fee, ticket price, or other origin] and get that to you before the
> letters go out.

It says it does not know, and leaves a bracket rather than inventing a reason.
Flagged `unsourced_claim` and `repeat_ask`, at `status = 'draft'`, nothing staged
or sent. **A bracket in an answer draft is correct** — the answer goes to Erica to
finish. A bracket in a *letter* is not, and `letters.return_document` 422s on one,
because that goes to a donor.

### The matcher met its first real gift — 28 Aug, 21:05 UTC

The 21:05 `gift-scan` staged a gift and resolved the donor, which is the thing
v3 was built for and the last part of it not yet proven in the wild:

| | |
| --- | --- |
| Subject | "Fwd: Gift of Mutual Fund" |
| Donor | Jerry Hulick |
| Amount | $10,000 |
| Designation | Gala sponsorship |
| Method | `in_kind` — transfer of mutual fund shares |
| Matched | **account 3699**, `sender_map`, score **1.00** |
| Flags | `gift_scan`, `sponsorship`, `date_missing` |

**The email was forwarded by `debi@sparcsolutions.org`, and the match came from
`jhulick@financialguide.com`.** That is the Micron failure mode — matching the
forwarder instead of the donor, and filing a gift onto Debi's own record — not
happening, on live mail. The mapping checks out independently: `crm_sender_map`
points that address at 3699, and account 3699's own `primary_email` is the same
address.

Score 1.00 is above `CONFIRMED`, so `match_constituent_id` was populated,
`match_candidates` left null and no `match_needs_confirming` flag raised. Correct
on both halves of the rule.

Two things about this gift are **not** settled, and the scanner was right to
leave both alone:

- `gift_date` is null and `date_missing` is flagged, because the email does not
  state one.
- The $10,000 is a **commitment**, and the model said so in its notes: *"exact
  final value depends on share price at transfer."* A share transfer is not worth
  its pledged figure until it settles. Nothing should receipt $10,000 until the
  actual proceeds are known.

It also lands squarely on the two decisions still waiting on Erica. It is a
**gala sponsorship**, so a letter for it needs the fair market value question
answered. And it is in `crm_inbox`, where approving posts a *note* — so as things
stand **this $10,000 will not become a thank-you letter at all.** That open
question now has a concrete number against it.

### Autodraft ran unattended — 28 Aug, 21:10 UTC

First firing on cron: **9 drafted, 0 refused, 1 failed**, `remaining` 395 → 385.
All 13 drafts sit at `status = 'draft'` with no `sent_at` and no
`gmail_draft_id`; nothing was sent or staged.

The failure was task 430 with a 502, *"Model did not return JSON"* — transient,
after `asks.generate`'s own internal retry. It is **counted, not swallowed**, and
the task still has no answer, so the next scheduled run picks it up. No fix
needed.

One number worth keeping an eye on: ten drafts took **79.5s against a 100s
deadline**. The design degrades gracefully — it stops and reports `stopped_early`
rather than being killed — but `limit: 10` is near the ceiling, so a slower model
day will start truncating runs rather than failing them.

---

## 10. The refusal paid for itself — 29 Aug

Overnight, Bloomerang went from **1,677 transactions to 1,775**. The 07:45
promoter did what it should with them: **9 promoted, 10 unnamed, 0 invented.**

Those ten are the whole argument for the rule. Their accounts were not yet in
`crm_account_map`, so `donation-sync` skipped them and said why — rather than
staging them under a placeholder that would eventually reach a tax receipt:

| Amount | Date | Donor, once known |
| ---: | --- | --- |
| $40,000 | 6 May | Foundation for Children with Intellectual and Developmental Disabilities |
| $9,273 | 29 May | McLean Community Foundation |
| $2,431 | 29 Jan | Jeanette Pagliuco |
| $1,500 | 21 Apr | MNM of Chantilly LLC |
| $1,000 | 29 May | Artisan Plumbing |
| $1,000 | 1 Apr | Renaissance Charitable Foundation |
| $1,000 | 18 Mar | Tracy Key |
| $500 | 17 Jun | Michael Fell |
| $200 | 15 Apr | Miles Macintyre |
| $100 | 1 Apr | James Kickpatrick |

**$57,004**, and a $40,000 foundation grant at the top of it. A guessed donor
name on that one would have been the worst possible failure of this system.

The remedy is the one the function names in its own skip message: run
`bloomerang sync_accounts`. That is the weekly Sunday cron (jobid 5), so it
would have healed itself the next morning — but $57k of unthankable money is
not worth sleeping on. Ran it by hand: 1,720 constituents upserted over 35
pages, all ten accounts resolved, then `donation-sync` again → **10 promoted,
0 unnamed.**

### What the owed list looks like now

| | 28 Aug | 29 Aug |
| --- | ---: | ---: |
| Gifts owed a letter | 62 | **81** |
| Value | $47,501.09 | **$108,952.98** |

Nineteen gifts and **$61,451.89** surfaced in one morning that the dashboard
could not previously see.

### Two things this run taught about the loaders

`sync_accounts` reports `total: 1806` but returns nothing past `skip: 1750`, so
it says `complete: false` forever — the same paging artifact as the transaction
mirrors, and the same lesson: **judge these loaders by whether the rows are
there, never by `complete`.**

The 07:30 snapshot recorded `unmatched_accounts: 20` because it ran *before* the
account sync. Those rows keep a null `account_number` in `snap_transactions`
until the next run re-reads them. It is cosmetic — `donation-sync` resolves on
`bloomerang_account_id` against `crm_account_map`, not on the mirror's own
column, which is why promotion worked at once. Worth knowing before someone
treats that 20 as a second bug.

### The acknowledgements mirror lags every weekend — 29 Aug midday

By midday `bloomerang_acknowledgments` was at **1677 against the snapshot's
1775**, 98 rows behind. Not the one-row bug from yesterday: `sync-bloomerang-ack`
runs `0 11 * * 1-5`, **weekdays only**, and 29 Aug was a Saturday. The daily
snapshot had picked up the overnight growth; the weekday-only ack job had not.

One default `{"max_pages":8}` call closed it — newest-first, so 400 rows covered
all 98. Both mirrors now hold **1,775**.

Where 2026 stands in Bloomerang's own acknowledgement field afterwards:

| | Gifts | Value |
| --- | ---: | ---: |
| Acknowledged | 31 | $65,321.68 |
| **Not acknowledged** | **44** | **$41,556.30** |

This is a schedule gap rather than a fault, but it is worth writing down twice
over: `acknowledged` is the field that says whether a donor has been thanked, and
a Saturday reader comparing the two mirrors will otherwise reach for the one-row
walk and conclude something is broken.

### Midday scans, all clean

12:00 tasks scan, 12:05 gift-scan, 12:10 autodraft: all 200, zero errors. The
gift scan examined 15 and staged nothing; the task scan found no new work in 26
messages. Autodraft drafted 9 and failed 1 again — the same transient shape as
last night, counted rather than swallowed and retried next run. The Hulick row is
untouched at `needs_review`, as it should be until the FMV and the
`crm_inbox`-to-donation questions are answered.

---

## 11. The Anthropic API ran out of credit — 29 Aug, between 12:10 and 21:10

The 21:10 autodraft run came back **0 drafted, 0 refused, 10 failed**, every one
of them:

> `Anthropic 400: {"type":"invalid_request_error","message":"Your credit balance
> is too low to access the Anthropic API. Please go to Plans & Billing to upgrade
> or purchase credit"}`

This is not a bug in anything here. It is the billing state of the account behind
`ANTHROPIC_API_KEY`, and **only Erica can clear it.**

### What stops working while it lasts

Every part of this system that *reads* uses a model call, so all three extraction
paths are down:

| Function | What it cannot do |
| --- | --- |
| `gift-scan` | detect a gift in a new email |
| `tasks` `scan` | pull a task out of a new email |
| `asks-autodraft` / `asks.generate` | draft an answer |

The 21:00 and 21:05 runs both reported `errors: 0`, and that is misleading: they
examined 26 and 15 messages respectively and **found every one already decided,
so neither spent a model call.** A clean scan result during an outage means only
that no new mail arrived — not that extraction works. The first genuinely new
email is where it will show.

### Nothing is being lost, and that is by design

- **`gift-scan` does not tombstone on a model error.** The error path pushes the
  failure into `results` and moves on, leaving the message undecided, so it is
  re-judged on the next run once credit returns. Only a real model *verdict*
  writes a `source_tombstones` row.
- **`asks-autodraft` counts failures rather than swallowing them.** A failed task
  keeps no answer, so it is still a candidate next run.
- **The task scanner does NOT behave this way. See §14 — it drops the message.**

So for the gift and answer paths the pipeline is **paused, not damaged**. The
task path needs one manual step when credit returns.

### Watch for this shape

A failure that looks transient can be the first sign of this. Task 430 failed on
21:10 the previous night with *"Model did not return JSON"* and again at midday —
easy to read as a quirk of one odd task. Tonight it failed with the credit error
alongside nine others. **A single repeating failure in an otherwise healthy run
is worth reading, not just counting.**

**Still out on 30 Aug 08:00.** One `{"limit":1}` probe, same error. Not retried
further. Meanwhile the non-model half of the pipeline ran perfectly overnight,
which is worth saying plainly: the outage costs new extraction, not the Bloomerang
side.

---

## 12. The owed list is mixing four kinds of revenue — 30 Aug

The Sunday sequence worked exactly as designed. `sync-constituents` fired at 07:00
(1,724 constituents), so when the promoter ran at 07:45 it had every name it
needed: **12 promoted, 0 unnamed** — no repeat of Saturday's refusals.

But the owed list jumped from 81 gifts / $108,952.98 to **93 / $386,613.30**, and
that $277,660 is the finding. Four of the twelve are this:

| Donor | Amount | Date |
| --- | ---: | --- |
| Department of Behavioral Health and Developmental Services | $20,833.33 | 17 Aug |
| Department of Behavioral Health and Developmental Services | $20,833.33 | 13 Jul |
| Department of Behavioral Health and Developmental Services | $20,833.33 | 20 Mar |
| Department of Behavioral Health and Developmental Services | $20,833.33 | 11 Mar |

Across the whole mirror there are **nine of them, every one exactly $20,833.33,
$187,499.97 in total, Oct 2025 to Aug 2026.** That is $250,000 a year paid
monthly — a **state contract draw**, not nine charitable gifts. It should not
receive a donor thank-you letter, and it must not receive a receipt saying *"no
goods or services were provided in exchange for this contribution."*

### Bloomerang already distinguishes these, and `donation-sync` ignores it

The `fund` field says so outright. 2026 donations by fund:

| Fund | Gifts | Value |
| --- | ---: | ---: |
| Grant Income | 6 | $204,100.00 |
| **Appropriated Funds** | 6 | **$124,999.98** |
| Unrestricted | 72 | $55,854.98 |
| Sponsorship | 6 | $41,500.00 |

The DBHDS payments are all `fund = "Appropriated Funds"`, `method = Check`,
account 4147. `donation-sync` promotes on `transaction_type = 'Donation'` alone
and never reads `fund`, so appropriated money lands in the owed list beside a
$50 individual gift.

Four funds plausibly want four different treatments — a donor letter, a grant
acknowledgement, a sponsorship letter carrying a fair market value, and for
appropriated funds probably no letter at all.

**Nothing was changed and no row was excluded.** Which funds are thankable is
Erica's call, not a rule to infer: guessing here either sends a state agency a
tax receipt it should not have, or silently drops revenue she does want
acknowledged. The lever is ready when she decides — `classify()` in
`donation-sync` already reads the transaction and would only need to consult
`fund`.

---

## 13. The mirror holds two rows Bloomerang no longer returns — 31 Aug

The durable check finally read the *other* way. `snap_transactions` holds **1,797
against a reported total of 1,795** — two rows too many, stable overnight, so not
a concurrent-write artifact.

`loaded_at` identifies them, because an upsert stamps it on every row it touches:

| Transaction | Donor | Amount | Gift date | Last seen |
| ---: | --- | ---: | --- | --- |
| 463873 | Google | $75,000.00 | 28 Mar 2025 | 28 Aug |
| 21674017 | Sumeet Shrivastava | $1,291.39 | 18 Feb 2026 | 29 Aug |

Every other row was re-stamped by Sunday's full walk (1,395 rows, `skip` 400 →
1800, `complete: true`) or Monday's newest-400. Those two together cover skip
0–1800 against a total of 1,795, and **neither pass returned these two ids.**

### Why this matters

`snap:21674017` is `pending` in `donations_staging` — **it is in the owed list,
queued for a thank-you letter.** If the transaction is gone from Bloomerang, that
letter would thank a donor for a gift the CRM no longer records.

(The Google row is not affected: the promoter's window starts 2026-01-01, so only
the separate Feb 2026 Google gift of $150,000 reached the owed list.)

### Two explanations, and this does not settle between them

1. **The transactions were removed or merged in Bloomerang.** Note that 463873
   and 6076418 are six- and seven-digit ids among neighbours in the 21,6xx,xxx
   range — the shape of records touched by a constituent merge, which can
   re-issue transaction ids and orphan the old ones here. The mirror upserts and
   never deletes, so an orphan lingers forever.
2. **Bloomerang's paging skipped them.** The walk pages `orderBy=Date&
   orderDirection=Desc` over 28 requests. Any write during those seconds shifts
   every later row by one, and a shifted row can fall between two pages. That
   would explain a miss with nothing deleted.

**Nothing was deleted here, and no staging row was touched.** Removing a
`pending` gift on inference is exactly the wrong move: if explanation 2 holds,
that silently drops a real $1,291.39 gift from the owed list.

**To settle it, look up transaction 21674017 in Bloomerang directly** before any
letter for it goes out. A single-transaction read would also make a proper
reconciliation possible — the mirror currently has no way to distinguish "gone"
from "missed", which is the real gap this exposed.

---

## 14. Correction: the task scanner loses a message that fails extraction

On 29 Aug this audit said the task scanner's error behaviour was "not verified
and should not be claimed". It is verified now, and the answer is the bad one.
**Mail that fails extraction is dropped permanently, not retried.**

The dedupe gate tests only whether a row exists:

```ts
const { data: judged } = await db.from("task_scan_messages")
  .select("message_id").eq("message_id", msg.id).maybeSingle();
if (judged) { run.skipped++; return; }
```

and the error path writes one:

```ts
if ((out as any).error) {
  run.errors.push(msg.id + ": " + why);
  await db.from("task_scan_messages").insert({ ...seen, tasks_found: -1, note: why.slice(0, 500) });
  return;
}
```

`tasks_found: -1` marks it as an error, but nothing ever reads that. The comment
above the insert states the intent and the escape hatch outright: *"Record the
failure too, or a message the model cannot parse burns calls out of every future
budget forever. Deleting the row requeues it."*

That is the right trade for a message the model can **never** parse. It is the
wrong trade for a **transient** failure, and the credit outage is transient.

### It has already cost real mail

Two messages were dropped this way at 12:00 on 31 Aug, the first new mail to
arrive during the outage. One of them:

> `1a054b8b1af877d9` — "Re: Correction - Join us at An Evening to SPARCle -
> November 14th"

A gala RSVP thread. Any ask inside it is gone from Today unless the row is
removed.

### The remedy, and when to run it

The requeue is exactly what the comment says — delete the marker rows:

```sql
delete from task_scan_messages where tasks_found = -1;
```

**Do this after credit is restored, not before**, or the next scan fails on them
again and re-inserts the same rows. Two rows are waiting as of 31 Aug 12:00;
`select count(*) from task_scan_messages where tasks_found = -1` is the number
outstanding at any time. Nothing was deleted yet, deliberately — the outage is
still live.

### The code change worth making, which is Erica's call

Only tombstone what will fail again. A parse failure or a `max_tokens` truncation
is permanent and should keep its row; an HTTP 429, a 5xx, or this credit error is
transient and should leave the message unjudged so the next pass retries it. As
written, one bad afternoon of API trouble silently costs a day of Debi's asks —
and it does it quietly, because `messages_skipped` counts a dropped message the
same way it counts a message with genuinely no task in it.

**Not deployed.** It is a live function on the task path and the trade-off is a
real one to choose, not to infer.

### Unverified, same risk class

`daily-sweep` reported **11 errors out of 13 examined** at 11:00 the same
morning, all the credit error. Whether it records failures the same way has
**not** been checked. It has its own dedupe table and its own error path, and it
owns Kat's check emails and the guest queue — so it is worth checking before the
next outage, not assumed either way.

---

## 6. Netlify, GitHub, live

- Production `sparc-donor-ops.netlify.app/devdash/` serves byte-identical
  `app.js` and `app.css` to `origin/main`. Correct content types, `no-store` on
  the three app files.
- `main` = `756bbd3`, which includes the `loadTasks` fix (PR #17, merged).
- 18 Edge Functions. `letters` is at v5 as of this session; `donor-ops` v9,
  `bloomerang` v11, `asks` v5, `docs` v2, `gift-scan` v2, `tasks` v3.
- `verify_jwt` is **false** on every dashboard function, deliberately: the app
  uses its own bcrypt login and server-side sessions, not Supabase JWTs. Two
  public-website functions (`virtual-summit-register`, `volunteer-register`)
  have it true. Do not "fix" the false ones.
- Supabase security advisors: 8 ERROR, 24 WARN, 71 INFO. The 71 INFO are all
  `rls_enabled_no_policy`. **Not urgent by itself** — nothing reaches these
  tables except Edge Functions holding the service role key, which bypasses RLS
  — but it means there is no second line of defence if the publishable key ever
  gains table access. Worth a pass; not done here.

### Google is not scanning the dashboard

Checked both hops:

| | |
| --- | --- |
| `sparc-donor-ops.netlify.app/devdash/` | `X-Robots-Tag: noindex, nofollow, noarchive`; `<meta name="robots" content="noindex, nofollow">`; `robots.txt` = `Disallow: /` |
| `sparcsolutions.org/devdash/` (GitHub Pages) | `<meta name="robots" content="noindex, nofollow">`, and `Disallow: /devdash/` in that site's `robots.txt` |

Also confirmed: `sparcsolutions.org` is served by GitHub Pages (301s
`/devdash` → `/devdash/`, then forwards client-side). Not a proxy, as
CLAUDE.md says. Do not turn it into one.

### Extraction is a model call, not a Google heuristic

Gmail is used only to **select** candidate messages (a wide keyword query), never
to decide anything. Every judgement is an Anthropic call:
`gift-scan.classify`, `asks.generate`, `docs.extractInstructions`,
`docs.revise`. `gift-scan`'s header records that `daily-sweep`'s money gate used
to be three regexes and that this is why gifts were mis-parsed; there is no
regex fallback left on the gift path. Keep it that way.

---

## 7. Known-bad artifact

`TransUrbanThankYou edtis.docx` (Drive, 26 Aug). Its last line reads:

```
Online Donation: $XXXX Date Received: XXX EIN: 20-55130060
```

Three faults, and the third is the serious one: **the EIN is wrong.**
`20-55130060` has an extra digit; it is `20-5513060`. It also breaks Debi's own
letter rules 4 and 13, and omits the "No goods or services were provided"
sentence entirely.

**The generator did not produce this.** A generated letter carries the correct
EIN, the full disclaimer, the address and the website — verified against the one
real letter in `letter_drafts` and against the live template. This file is
hand-authored. The bad EIN appears in exactly one file across the whole Drive.

Not repaired: **Transurban is in no table in Supabase** — no sponsorship, no
constituent, no donation — so the amount and date behind those `XXXX` are not
recorded anywhere and must not be invented. It needs the real figures, then it
can be regenerated properly through `letters.generate`.

---

## 8. What still needs doing

1. ~~`gift-scan` should record the constituent match when it stages a gift~~ —
   **done 28 Aug, v3** (§2). Only a score ≥ 0.9 populates
   `match_constituent_id`; a weaker hit becomes a `match_candidates` suggestion
   flagged `match_needs_confirming`, so a name-only guess still has to be
   confirmed before a gift lands on a donor record.
2. ~~Answers pre-drafted rather than drafted on click~~ — **deployed 28 Aug**,
   `asks-autodraft` v1, on cron at 12:10 / 16:10 / 21:10 with `limit: 10`, the
   agreed scope. It calls `asks.generate` over HTTP so each draft gets its own
   worker invocation and its own resource limit. First live run drafted 3 of 3
   with 0 refused and 0 failed in 20s. **398 open asks still have no draft** —
   three runs a day keep the front of the queue warm rather than clearing it,
   and `remaining` in the response is the honest count.
3. The Transurban letter, once the amount and date are known (§7).
4. `donor-ops.update` / `dismiss` / `note` have no caller (§3) — the flag
   resolution path.
5. RLS policies (§6).

## Verified live, end to end

Against the real backend, with a real gift:

```
generate  Carol Benner, $25, 2026-07-23
          address_source:    bloomerang
          address_how:       cluster survivor of 2 records
          matched_account:   3813
          address_lines:     713 Maiden Choice Ln / Apt 2407 /
                             Catonsville, Maryland 21228-3895
          thank_you_letter:  created
draft_batch  1 letter, one Gmail draft to Debi, nothing sent
owed         44 -> 43   Carol Benner off the list
```

That single run exercises the whole chain that was broken: the dead button, the
missing address, the duplicate-safe account resolution, and the open loop.

Also confirmed live: `asks.approve` moves a draft to `staged` and `asks.unstage`
moves it back (state left exactly as found); `bloomerang.approve` on the Micron
row returns the "No constituent matched" 400 and mutates nothing;
`bloomerang.match` resolves all four queued donors.

## How this was verified

A short-lived `app_sessions` row was minted against the existing
`system:tasks-cron` account and used to call the live functions with curl, then
deleted. Audit rows from this pass are attributed to `system:tasks-cron`, not to
Erica.

Browser tests run against a local server with the Supabase origin intercepted.
Chromium in that environment cannot reach the live API (it has no CA for the
egress proxy, and disabling TLS verification is not acceptable), so UI tests use
response shapes **captured verbatim from live calls**, and the backend is
exercised directly with curl. Where a result below says "live", it was curl.

---

## 9. The chain from a gift to a letter — 27 Aug, later

Erica: "scan my email, give me a draft note for Bloomerang I approve with a
button, and write the thank-you letter automatically." Three pipelines, and the
middle one did not exist.

| Link | State |
| --- | --- |
| Bloomerang → `snap_transactions` | works. 1,676 txns, synced 07:30 daily. |
| email → `crm_inbox` (Bloomerang tab) | works. `gift-scan`, 3×/day. |
| **Bloomerang → the owed list** | **was broken.** See below. |
| owed list → letter → Debi | works (fixed earlier today). |
| `crm_inbox` → the owed list | **no path, by design.** Approving there posts a *note*, not a donation, so an emailed gift never becomes a letter. |

### Why new donations never appeared

`donations_canonical` is a **VIEW**: `donations_staging` where
`status='pending' AND superseded_by IS NULL`. Nothing had written to
`donations_staging` since **14 Aug**. No cron job writes it — the ten scheduled
jobs feed the mirror, the acknowledgements, the account map, the task scan and
the gift scan, and none of them this. It was loaded once and abandoned.

Consequence: **Victor Hoskins gave $25,000 on 18 Aug** — a gala Event Sponsor
gift with 10 tickets, against prior gifts of $10,000 in 2023 and 2024 — and the
dashboard never showed it. Also a $516.83 gala ticket purchase on 20 Aug.

Both are now staged (`source='snapshot'`, `source_id='snap:<txn>'`,
`status='pending'`) and appear in the owed list. Dedupe is by `source_id` **and**
by `donation_fingerprint(donor_name, amount, donation_date)` — without the
fingerprint check a snapshot row duplicates the `bloomerang_form` row for the
same gift. A dry run over all 2026 donations showed every gift back to 11 Aug
correctly recognised as already staged, and exactly these two as new. Five
earlier `source='snapshot'` rows exist from a 14 Aug attempt; all were set
`declined`, so they must be skipped by `source_id`, not re-added.

**Done**: `donation-sync`, deployed and scheduled as cron job
`promote-donations` at **07:45 UTC daily**, right after `bloomerang-snapshot`
loads the mirror at 07:30.

Verified through the cron path itself, not just curl —
`select call_edge_svc('donation-sync', ...)` returned request 152, and
`net._http_response` shows **200** with `ok:true`. That is the check
`## 5` insists on: pg_cron reporting "succeeded" proves nothing.

It never invents a donor. A transaction whose account resolves to no name is
skipped and **reported** under `unnamed`, because `donor_name` is NOT NULL and a
placeholder would flow onto a tax receipt. The fix for those is
`bloomerang sync_accounts`, which is exactly how Virginia Bulger got a name.

Running it surfaced **four more gifts that had never reached the owed list**,
beyond the two found by hand:

| Donor | Amount | Date | Tickets |
| --- | --- | --- | --- |
| Sumeet Shrivastava | $5,165.59 | 13 Feb | 2 |
| Warren Thompson | $5,000.00 | 15 Apr | 2 |
| Joshua Harless | $1,033.36 | 30 Jun | 2 |
| Larysa Kautz | $1,033.36 | 1 Jul | 2 |

With Hoskins and Bulger that is **$37,749.14 in gifts the dashboard had never
shown**. The owed list went 43 → **49**, worth $43,606.30. A second run promoted
0 and skipped 56, so the dedupe holds.

Every one of the four carries tickets, so all four are blocked by the quid pro
quo guard below until a fair market value is set.

### QUID PRO QUO — the disclaimer was wrong on every gala gift

The 501(c)(3) line was unconditional:

> No goods or services were provided in exchange for this contribution.

That is **false** for a gift that came with gala tickets, and it is on a tax
receipt. **Seven 2026 gala gifts include 1 to 10 tickets each**, among them the
$25,000 Hoskins sponsorship with ten. Bloomerang records
`NonDeductibleAmount: 0` on all of them, so the figure is not available there
either. A receipt that understates goods received overstates the donor's
deduction.

One such letter had already been drafted to Debi (Sally Crosen / Maid Brigade,
$500, 2 tickets). It is a Gmail draft, not sent to the donor, so it is
recoverable. Erica's own `thank_you_letters` note on it reads "FMV disclaimer
variant" — she already knew a different disclaimer was needed.

`letters` v7: `disclaimer()` now takes `goods_value` and states the fair market
value and the deductible amount. A gift with `ticket_quantity > 0` and no
`goods_value` is **refused with 422**, verified live against the Hoskins gift.
The value of a ticket is SPARC's good-faith estimate — this function must not
invent it.

**Blocked on one number: the fair market value per gala ticket.** With it, every
gala letter can be written correctly.

### Transurban / Ginny — the missing figures, found

Refreshing `crm_account_map` (`bloomerang.sync_accounts`, 1,703 upserted)
resolved the unnamed 20 Aug donor: **Virginia Bulger, vbulger@transurban.com,
account 4125**. That is the Ginny of `TransUrbanThankYou edtis.docx`, whose
receipt line reads `Online Donation: $XXXX Date Received: XXX`.

The real figures: **$516.83, received 20 August 2026**, a Friend-level gala
sponsorship including **2 tickets**. So that letter needs the FMV line too, and
its EIN corrected from `20-55130060` to `20-5513060`.
